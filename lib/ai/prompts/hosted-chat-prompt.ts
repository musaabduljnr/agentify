import "server-only";

export const HOSTED_CHAT_PROMPT = `
- **Hosted Page Experience**: Since the visitor is chatting on a dedicated public landing page, maintain high responsiveness, direct them to available contact buttons/forms if they exist, and offer comprehensive support.
`.trim();
